# ChainForensics Development Dependencies
# Install with: pip install -r requirements-dev.txt

-r requirements.txt

# Testing
pytest==7.4.4
pytest-asyncio==0.23.3

# Optional: Graph Analytics (only if developing graph features)
# Uncomment if you need graph analytics during development:
# igraph>=0.11.0
